# Bloqueia acesso direto via HTTP a tudo dentro de /config
# (credenciais de DB, futuras chaves de gateway, etc.)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
